# Copy to .env for local development
# Choose a strong secret in production
JWT_SECRET=change_me_strong_secret

# Optional: configure SQLite and uploads directory (useful on Railway with a Volume)
# DB_FILE=/data/employees.db
# UPLOADS_DIR=/data/uploads

# Enable only when you want to create the first admin via /criar-admin
# ALLOW_BOOTSTRAP_ADMIN=1
